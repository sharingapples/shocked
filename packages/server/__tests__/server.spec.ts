// import { Server, Session } from '../src';

// type User = {
//   id: string,
//   name: string,
// }

// function getUser(id: string): User {
//   return {
//     id, name: 'John Doe',
//   };
// }

describe('Server Specification', () => {
  it('checks for server listening', async () => {
    console.warn('no tests yet. Write some');
    // const port = 19098;
    // const server = new Server<User>();
    // server.track('/sock', {
    //   api: {},
    //   onIdent: async () => {
    //     return getUser('1');
    //   },
    //   onStart: async (session: Session<User>) => {
    //     console.log('Session has started', session.user);
    //   }
    // });

    // const shutdown = await server.start(port);
    // shutdown();
  });
});
