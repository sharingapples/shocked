{
  "name": "shocked-channel-redis",
  "version": "0.2.3",
  "description": "Redis based channel for redsock",
  "main": "cjs/index.js",
  "scripts": {
    "lint": "../../node_modules/.bin/eslint src",
    "clean": "../../node_modules/.bin/rimraf cjs",
    "build": "../../node_modules/.bin/babel src -d cjs",
    "prepare": "npm run lint && npm run clean && npm run build"
  },
  "author": "Ranjan Shrestha",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "redis": "^2.8.0"
  }
}
